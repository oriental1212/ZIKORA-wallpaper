# Test fixtures

This directory is reserved for deterministic, locally generated test inputs.

Fixtures must not depend on the public internet, a developer's home directory,
personal data, or copyrighted production images. P01-05 will add the image
validation fixture manifest and generated binary samples.
