# ZIKORA-wallpaper 需求规格说明书

> 文档版本：V1.0  
> 产品版本：MVP V1.0  
> 适用平台：macOS  
> 文档状态：可评审  
> 编制日期：2026-08-18  
> 输入依据：`ZIKORA-wallpaper_PRD_V1.0.md`、`prototype/` 原型图及对应 HTML  
> 目标读者：产品、UI/UX、macOS 客户端开发、测试

---

## 1. 文档目的

本文档将产品 PRD 与现有高保真原型统一为可设计、可开发、可测试、可验收的需求规格，重点明确：

- V1.0 的功能边界与优先级；
- 页面信息架构、控件行为和页面状态；
- 壁纸获取、缓存、切换、清理的业务规则；
- macOS 系统集成及后台运行要求；
- 数据模型、异常处理和验收标准；
- PRD 与原型不一致时的处理结论。

本文档不描述具体技术框架、数据库选型或代码实现方式。

---

## 2. 产品概述

### 2.1 产品定义

ZIKORA-wallpaper 是一款轻量级 macOS 自动壁纸工具。用户配置可直接返回图片的远程 URL 后，应用按照每周计划自动获取图片、保存到本地、设置桌面壁纸，并可循环使用保留期内的历史壁纸。

核心价值链路为：

```text
配置壁纸源 → 每周调度 → 自动获取 → 本地缓存 → 设置壁纸 → 循环利用 → 自动清理
```

### 2.2 产品目标

- 用户在 3 分钟内完成首次配置；
- 完成配置后，用户数周内无需频繁打开主窗口；
- 每日模式正常情况下每天只进行必要的网络请求；
- 空闲时 CPU 接近 0%，内存目标低于 80 MB；
- 网络、睡眠唤醒、日期变化或单个壁纸源异常时应用不崩溃；
- 本地缓存按保留策略自动治理，不无限增长。

### 2.3 目标用户

- 希望每天自动更换桌面壁纸的用户；
- 希望不同星期使用不同壁纸风格的用户；
- 希望循环使用历史图片、减少重复下载的用户；
- 偏好无账号、无广告、低资源占用工具的 macOS 用户。

### 2.4 设计原则

- 简单：核心设置路径短，概念和文案一致；
- 自动：以系统事件驱动，不依赖用户每天操作；
- 轻量：避免高频轮询、持续目录扫描和无意义动画；
- 本地优先：除壁纸源请求外，配置、图片和记录均保存在本机；
- 图片优先：当前壁纸是 Dashboard 的主要视觉焦点；
- 可恢复：失败不破坏当前壁纸，并向用户提供清晰的恢复入口。

---

## 3. 需求依据与冲突裁决

### 3.1 依据优先级

当输入材料存在不一致时，采用以下优先级：

1. PRD 中明确声明的 MVP 范围、业务规则和非目标；
2. 原型中与 PRD 一致且更具体的页面布局、控件和文案；
3. 本文档为保证流程闭环而补充的交互及异常规则。

### 3.2 PRD 与原型差异处理

| 编号 | 差异 | V1.0 处理结论 |
|---|---|---|
| D-01 | 原型 Sources 页面出现 `Local Collection` 本地目录源；PRD 仅定义可直接返回图片的远程 URL | 本地目录源不进入 V1.0。该卡片仅作为“停用/离线源”视觉参考；V1.0 数据模型不增加目录权限字段 |
| D-02 | 原型 Weekly Plan 画面仅展示 Monday～Friday | 实际功能必须展示并保存周一至周日 7 天配置，画面可滚动或完整展开 |
| D-03 | PRD 描述默认源用于当日未配置或来源停用；原型文案描述为计划更新失败时兜底 | V1.0 统一为三种情况均可兜底：未配置、来源停用、主来源本轮请求最终失败；每次任务只允许尝试默认源一次 |
| D-04 | 原型 Library 有搜索框，PRD 仅要求网格、详情和设为当前壁纸 | 搜索/筛选列为 P1；若 V1.0 实现，支持按来源名称和下载日期过滤，不要求全文检索 |
| D-05 | 原型设置仅提供永久、30 天、7 天；PRD 还定义 14/60/90 天 | 以 PRD 为准，提供 7、14、30、60、90 天及永久，默认 30 天 |
| D-06 | 原型有顶部刷新、更多菜单，但未定义行为 | 顶部刷新等同“立即更新”；更多菜单承载低频操作，菜单项见 10.1.4，不允许保留无响应控件 |
| D-07 | 原型同时使用 `Quick Update`、`Update Now`、刷新图标 | 中文产品统一文案为“立即更新”；含义为强制重新请求今日来源，不等同“换一张” |
| D-08 | 原型同时使用 `Next Wallpaper`、`Change Next` | 中文产品统一文案为“换一张”；循环模式从本地选下一张，每日模式也允许从本地历史中临时切换 |
| D-09 | PRD 要求 Menu Bar，现有四张主窗口原型未覆盖 | Menu Bar 仍为 V1.0 P0，按本文档 11 章实现并补充设计稿 |
| D-10 | PRD 有 Onboarding、壁纸详情及异常状态，原型未覆盖 | 仍在 V1.0 需求范围内，按本文档补充页面/状态设计 |

---

## 4. 产品范围

### 4.1 V1.0 范围

| 模块 | 能力 | 优先级 |
|---|---|---|
| 壁纸源 | 添加、编辑、删除、启停、测试直链图片源 | P0 |
| 每周计划 | 周一至周日来源分配、默认来源 | P0 |
| 自动获取 | 启动、登录、唤醒、跨日事件检查并下载 | P0 |
| 缓存 | 本地保存、元数据、Hash 去重、过期清理 | P0 |
| 展示模式 | 每日壁纸、循环播放、随机/顺序及间隔 | P0 |
| 系统集成 | 设置桌面壁纸、统一多屏、登录启动、后台运行 | P0 |
| Menu Bar | 查看状态、立即更新、换一张、打开主窗口、退出 | P0 |
| 主窗口框架 | Dashboard、Sources、Library、Settings 导航及页面框架 | P0 |
| 历史壁纸 | 网格、详情、设为当前、Finder 定位、删除 | P1 |
| 状态提示 | 空、加载、离线、失败、停用、警告、缺少配置 | P0 |
| 搜索筛选 | 按来源或日期筛选壁纸库 | P1 |

### 4.2 V1.0 非目标

- 动态或视频壁纸；
- AI 壁纸生成；
- 用户账号、云同步、社区、商城；
- RSS、HTML、JSON Path 等内容解析；
- 本地目录作为壁纸源；
- 不同显示器使用不同壁纸；
- 收藏、评分、黑名单；
- 来源优先级链、多级备用来源；
- iCloud 同步。

---

## 5. 术语与统一文案

| 术语 | 定义 |
|---|---|
| 壁纸源 / Source | 一个启用后可直接返回静态图片文件的远程 URL 配置 |
| 今日来源 | 根据系统本地日期和星期从周计划中选中的来源 |
| 默认来源 | 今日来源不可用或获取失败时最多尝试一次的兜底来源 |
| 每日壁纸 | 每个自然日自动获取一次今日来源最新图片的模式 |
| 循环播放 | 按设定间隔从有效本地缓存中切换图片的模式，不发起网络请求 |
| 立即更新 | 忽略“今日已成功获取”的限制，重新请求今日来源 |
| 换一张 | 立即从本地有效壁纸中选择下一张并设置为桌面壁纸 |
| 当前壁纸 | 应用最后一次成功设置到 macOS 桌面的图片 |
| 有效本地壁纸 | 文件存在、可解码、未过期或受当前壁纸保护的缓存图片 |

界面不得混用“快速更新”“同步”“下一张”“更换下一张”等多套文案。

---

## 6. 用户角色与典型场景

V1.0 仅有“本机用户”一个角色，无登录和权限分级。

### 6.1 首次使用

用户启动应用，添加并测试第一个壁纸源，确认默认设置，应用立即获取一张图片并设置为桌面壁纸。

### 6.2 每日自动更新

用户已配置每周计划。应用在当天首次满足触发条件时请求今日来源，成功后保存并设置壁纸，当天不再自动重复请求。

### 6.3 每周计划

用户将不同壁纸源分配给一周七天。应用依据 macOS 当前本地日期选择对应来源；计划缺失或来源停用时尝试默认来源。

### 6.4 历史循环

用户切换为循环播放模式。应用按随机或时间顺序，从保留期内的本地图片中按间隔切换，不产生网络请求。

### 6.5 异常恢复

网络不可用、图片无效或来源超时时，应用保留当前桌面壁纸，记录失败并按退避策略重试；用户可从主窗口或 Menu Bar 手动重试。

---

## 7. 总体信息架构

```text
ZIKORA-wallpaper
├── 首次引导
│   ├── 欢迎
│   ├── 添加首个壁纸源
│   └── 基础设置
├── 主窗口
│   ├── Dashboard
│   ├── Sources
│   │   ├── 壁纸源列表
│   │   ├── 添加/编辑壁纸源
│   │   ├── 每周计划
│   │   └── 默认来源
│   ├── Library
│   │   └── Wallpaper Detail
│   └── Settings
│       ├── General
│       ├── Wallpaper Rotation
│       ├── Storage Management
│       └── About
└── Menu Bar
```

主窗口采用固定侧边栏导航，侧边栏底部提供“立即更新”。顶部工具栏提供当前页面标题、上下文主操作、刷新及更多菜单。

---

## 8. 全局交互规则

### 8.1 窗口与后台

- REQ-GLOBAL-001：关闭主窗口只隐藏窗口，应用继续在 Menu Bar 后台运行。
- REQ-GLOBAL-002：仅“退出 ZIKORA-wallpaper”终止进程及后台任务。
- REQ-GLOBAL-003：登录后自动启动时默认不弹出主窗口。
- REQ-GLOBAL-004：主窗口重新打开时恢复上次访问的一级页面；首次打开默认进入 Dashboard。
- REQ-GLOBAL-005：窗口内容不足时保持稳定布局；内容超出时只滚动主内容区域，侧边栏和工具栏保持可访问。

### 8.2 全局操作

- REQ-GLOBAL-006：侧边栏“立即更新”、Dashboard“立即更新”和顶部刷新按钮执行相同命令。
- REQ-GLOBAL-007：同一个更新任务执行期间，所有更新入口进入禁用或进度状态，防止重复提交。
- REQ-GLOBAL-008：更新成功后 Dashboard、Sources 最近同步时间、Library 和 Menu Bar 状态同步刷新。
- REQ-GLOBAL-009：“换一张”只能从本地有效壁纸中选择，不触发网络请求。
- REQ-GLOBAL-010：操作结果通过不阻塞用户的状态提示呈现；删除、立即清理等不可逆操作使用确认对话框。

### 8.3 反馈时限

- 点击后 100 ms 内呈现按压、禁用或加载反馈；
- 普通本地操作 1 秒内完成时无需持续进度条；
- 网络操作超过 1 秒时显示加载状态；
- 成功提示可自动消失；失败提示必须包含原因和可执行的恢复动作。

---

## 9. 首次启动与引导

### 9.1 触发条件

- REQ-ONB-001：首次安装且不存在有效配置时展示首次引导。
- REQ-ONB-002：引导未完成前退出，重新启动后回到上次未完成步骤。
- REQ-ONB-003：用户至少成功保存一个已通过测试的壁纸源后才能完成引导。

### 9.2 步骤一：欢迎

显示：

- 产品标识与名称；
- 标题“让每天的桌面都有一点新鲜感”；
- 产品作用说明；
- 主按钮“开始设置”。

### 9.3 步骤二：添加首个壁纸源

字段：名称、壁纸 URL。操作：测试连接、继续、返回。

- 名称不能为空，去除首尾空格后长度建议为 1～50 个字符；
- URL 必须为合法的 `http` 或 `https` 地址；
- 测试成功后显示预览及图片基本信息；
- URL 或名称修改后，原测试结果失效；
- “继续”仅在当前 URL 最近一次测试成功后可用。

### 9.4 步骤三：基础设置

默认值：

- 登录后自动启动：开启；
- 模式：每日壁纸；
- 保留周期：30 天；
- 首个来源：应用到周一至周日，并设置为默认来源。

点击“开始使用 ZIKORA-wallpaper”后保存配置、进入 Dashboard，并发起首次今日壁纸任务。

---

## 10. 主窗口页面需求

### 10.1 Dashboard

#### 10.1.1 页面目标

让用户快速确认当前壁纸、应用运行状态、当前模式及今日来源，并完成最高频操作。

#### 10.1.2 页面内容

与 `prototype/dashboard_vibe_update` 对齐：

- 顶部工具栏：页面标题、“换一张”、刷新、更多；
- 当前壁纸大图：占据主要视觉区域；
- 图片信息浮层：来源名称、获取日期与时间；
- 主操作：“立即更新”“换一张”；
- 状态卡片：当前模式、今日来源。

#### 10.1.3 功能规则

- REQ-DASH-001：有当前壁纸时显示本地原图的等比裁切预览，不拉伸变形。
- REQ-DASH-002：来源已被删除时仍显示历史记录中的来源快照名称。
- REQ-DASH-003：“立即更新”触发 12.3 的手动更新流程。
- REQ-DASH-004：“换一张”触发 14.4 的本地换图流程。
- REQ-DASH-005：当前模式显示“每日壁纸”或“循环播放”。
- REQ-DASH-006：今日来源显示实际计划来源；使用默认来源兜底后显示“默认来源名称（兜底）”。
- REQ-DASH-007：无当前壁纸时展示空状态及“获取今日壁纸”操作。
- REQ-DASH-008：任务进行中在大图区域显示非遮挡式进度状态，不清除已有壁纸。

#### 10.1.4 更多菜单

- 打开壁纸所在位置；
- 打开设置；
- 关于 ZIKORA-wallpaper；
- 退出 ZIKORA-wallpaper。

与当前状态不适用的操作应禁用并给出原因，不显示空菜单。

### 10.2 Sources

#### 10.2.1 页面结构

与 `prototype/sources_vibe_update` 对齐，依次展示：

1. 工具栏“添加壁纸源”；
2. 壁纸源卡片列表；
3. Weekly Plan；
4. Default Source。

#### 10.2.2 壁纸源卡片

每张卡片显示：

- 来源名称；
- 经过安全截断的 URL；
- 启用开关；
- 最近同步时间；
- 最近同步状态；
- 编辑入口。

状态包括：从未同步、同步中、成功、离线、失败、连续失败警告、已停用。

- REQ-SRC-001：列表展示全部来源，不以“Active Sources”标题隐藏停用来源；中文标题统一为“壁纸源”。
- REQ-SRC-002：关闭来源开关后，该来源立即不再参与自动任务和计划选择。
- REQ-SRC-003：若停用来源已被某天计划引用，该天显示“来源已停用”警告，但不自动改写用户配置。
- REQ-SRC-004：若停用的是默认来源，默认来源区域显示不可用警告。
- REQ-SRC-005：重新启用后恢复原计划引用，不自动发起下载。
- REQ-SRC-006：点击 URL 可复制完整地址；V1.0 不要求在内置浏览器打开。

#### 10.2.3 添加/编辑壁纸源

使用 Sheet 或独立表单，字段如下：

| 字段 | 必填 | 规则 |
|---|---|---|
| 名称 | 是 | 去除首尾空格后不能为空；建议不超过 50 字符 |
| 壁纸 URL | 是 | 仅 `http`/`https`；必须是直接图片响应 |
| 启用 | 是 | 默认开启 |
| 图片预览 | 否 | 最近一次连接测试成功后显示 |

操作：测试连接、取消、保存。

- REQ-SRC-007：同名来源允许保存，但应提示可能难以区分；URL 完全相同时要求用户确认。
- REQ-SRC-008：新增来源必须测试成功后才能保存。
- REQ-SRC-009：编辑仅修改名称或启用状态时不强制重测；URL 发生变化时必须重新测试。
- REQ-SRC-010：取消编辑不保存任何修改。
- REQ-SRC-011：保存成功后关闭表单，并在来源列表中定位到新建或修改项。

#### 10.2.4 测试连接

流程：

```text
校验 URL → 发起请求 → 校验 HTTP → 校验大小 → 校验类型 → 解码图片 → 展示预览
```

- 请求超时建议为 15 秒；
- 单张图片上限 50 MB；
- 支持 JPG/JPEG、PNG、HEIC、WebP，最终以 macOS 可解码为准；
- 不执行 HTML 抓取、JavaScript 或 JSON 路径解析；
- 成功显示预览、尺寸、格式及文件大小；
- 失败显示网络不可用、超时、HTTP 错误、非图片、无法解码或文件过大等明确原因。

#### 10.2.5 删除来源

- 删除入口位于编辑表单或上下文菜单；
- 二次确认中显示来源名称及受影响的星期；
- 删除配置后，相关星期变为“未设置”；
- 删除默认来源后，默认来源变为“未设置”；
- 已下载的历史壁纸不删除，继续按保留策略管理；
- 删除完成后不得因悬空引用导致任务崩溃。

### 10.3 Weekly Plan

- REQ-PLAN-001：展示周一至周日七行，顺序固定。
- REQ-PLAN-002：每行可从“全部已启用来源 + 未设置”中单选。
- REQ-PLAN-003：选择结果即时保存；失败时恢复旧值并提示。
- REQ-PLAN-004：只有一个有效来源时，新用户默认将其分配到七天。
- REQ-PLAN-005：删除已引用来源后，相关日期显示“未设置”。
- REQ-PLAN-006：停用已引用来源后保留引用，但显示“来源已停用”。
- REQ-PLAN-007：当天行显示轻量“今天”标记。

### 10.4 Default Source

- REQ-DEF-001：默认来源只能从已启用来源中选择，也允许“未设置”。
- REQ-DEF-002：出现以下任一情况时尝试默认来源：当天未配置、计划来源不存在、计划来源已停用、计划来源本轮自动重试最终失败。
- REQ-DEF-003：若默认来源与失败的计划来源相同，不重复请求。
- REQ-DEF-004：一次任务最多尝试默认来源一次，不形成递归兜底。
- REQ-DEF-005：默认来源成功时，下载记录保存真实来源 ID，并标记本次任务为兜底成功。

### 10.5 Library

#### 10.5.1 页面结构

与 `prototype/library_vibe_update` 对齐：

- 顶部搜索/筛选框（P1）；
- “换一张”、刷新、更多；
- 响应式壁纸缩略图网格；
- 当前壁纸右上角显示选中标识，并使用主色描边。

每项显示缩略图、名称、下载日期、来源标签。

#### 10.5.2 列表规则

- REQ-LIB-001：默认按下载时间倒序排列。
- REQ-LIB-002：缩略图异步加载，保持固定比例，失败时显示占位图。
- REQ-LIB-003：当前壁纸必须有唯一明确标识。
- REQ-LIB-004：点击缩略图打开壁纸详情，不因单击直接更换系统壁纸。
- REQ-LIB-005：Library 刷新只重新读取本地记录和文件状态，不发起网络请求。
- REQ-LIB-006：文件丢失时将记录标为异常并从轮播候选中排除；可在刷新时清理无效记录。
- REQ-LIB-007：来源已删除时仍显示下载时保存的来源名称快照。

#### 10.5.3 搜索与筛选（P1）

- 输入来源名称或 `YYYY-MM-DD` 日期进行过滤；
- 匹配不区分英文大小写；
- 输入 300 ms 后执行本地过滤；
- 无结果时显示“没有匹配的壁纸”和“清除筛选”。

### 10.6 Wallpaper Detail（P1）

展示大图、来源、下载日期、图片尺寸、格式、文件大小和本地路径。

操作：

- 设为当前壁纸；
- 在 Finder 中显示；
- 删除。

规则：

- REQ-DETAIL-001：设为当前壁纸成功后更新唯一 `isCurrent` 标记和全局状态。
- REQ-DETAIL-002：当前正在使用的图片不允许删除，删除入口禁用并提示原因。
- REQ-DETAIL-003：删除非当前图片前二次确认；确认后删除文件和对应记录。
- REQ-DETAIL-004：Finder 定位失败时提示文件可能已被移动或删除。

### 10.7 Settings

#### 10.7.1 页面结构

与 `prototype/settings_vibe_update` 对齐，使用单页分区：General、Wallpaper Rotation、Storage Management、About。

所有设置变更即时保存；保存失败时恢复旧值并提示。

#### 10.7.2 General

“登录后自动启动”开关：

- 默认开启；
- 打开时注册 macOS 登录项；
- 关闭时移除登录项；
- 系统拒绝或注册失败时恢复开关并说明处理方式。

#### 10.7.3 Wallpaper Rotation

| 设置 | 可选值 | 默认值 |
|---|---|---|
| 模式 | 每日壁纸、循环播放 | 每日壁纸 |
| 顺序 | 随机、按时间顺序 | 随机 |
| 间隔 | 5 分钟、15 分钟、30 分钟、1 小时、3 小时、6 小时、12 小时、每天 | 30 分钟 |

- REQ-SET-001：每日模式隐藏或禁用“顺序”和“间隔”，并保留上次循环设置。
- REQ-SET-002：切换至循环模式后立即加载有效本地候选；不立即请求网络。
- REQ-SET-003：循环模式无候选壁纸时，模式保留但显示警告与“获取今日壁纸”入口，不创建空转定时器。
- REQ-SET-004：修改循环间隔后，从修改成功时重新计算下一次触发时间。
- REQ-SET-005：顺序播放按下载时间从旧到新，末尾回到第一张；不得连续选择当前图片。
- REQ-SET-006：随机播放在候选数大于 1 时不得连续重复当前图片。

#### 10.7.4 Storage Management

| 设置/信息 | 需求 |
|---|---|
| 保留周期 | 7、14、30、60、90 天、永久；默认 30 天 |
| 缓存位置 | 显示应用管理目录的用户友好路径 |
| 缓存占用 | 显示当前有效图片文件总大小 |
| 打开 | 在 Finder 中打开缓存目录 |
| 立即清理 | 按当前保留规则执行清理 |

- REQ-STORE-001：切换保留周期不立即删除文件，显示预计影响；自动清理在下次规定时机执行。
- REQ-STORE-002：点击“立即清理”前展示预计删除数量和空间；用户确认后执行。
- REQ-STORE-003：当前壁纸始终受保护，即使已超过保留期。
- REQ-STORE-004：清理失败不移除对应数据库记录，并列出失败数量。
- REQ-STORE-005：完成后刷新占用空间及 Library。
- REQ-STORE-006：“永久”表示不按时间删除，仍允许清理损坏记录或用户手动删除。

#### 10.7.5 About

展示应用名称、版本号、隐私说明入口；Feedback 和 GitHub 仅在存在有效目标地址时显示。

---

## 11. Menu Bar 需求

### 11.1 结构

```text
ZIKORA-wallpaper
当前壁纸缩略图
来源名称
获取日期与时间 / 当前状态
────────────────
换一张
立即更新
────────────────
打开 ZIKORA-wallpaper
设置
────────────────
退出 ZIKORA-wallpaper
```

### 11.2 行为

- REQ-MENU-001：Menu Bar 图标常驻，正常状态不持续播放动画。
- REQ-MENU-002：下载中可显示一次性进度状态，但不得高频刷新图标。
- REQ-MENU-003：“换一张”“立即更新”与主窗口同名操作完全一致。
- REQ-MENU-004：失败时显示简短原因和“重新获取”。
- REQ-MENU-005：“打开 ZIKORA-wallpaper”唤起主窗口并置前。
- REQ-MENU-006：“设置”唤起主窗口并定位至 Settings。
- REQ-MENU-007：“退出”终止应用，并取消内存中的循环任务和待执行重试。

---

## 12. 壁纸获取与下载

### 12.1 自动触发条件

每日模式在以下事件发生时检查今日任务：

- 应用启动；
- macOS 登录后启动；
- Mac 从睡眠恢复；
- 系统本地日期变化后的首次事件检查；
- 网络恢复且当天存在未完成任务。

不允许通过每秒或每分钟轮询日期实现。

### 12.2 自动获取流程

```text
接收触发事件
  ↓
读取系统本地日期与设置
  ↓
当前是否为每日模式？── 否 → 不请求网络
  ↓ 是
今天是否已有成功记录？── 是 → 必要时恢复本地当前壁纸，结束
  ↓ 否
解析今日计划来源
  ↓
来源有效？── 否 → 尝试默认来源
  ↓ 是
请求并验证图片
  ↓
计算内容 Hash
  ↓
已存在？── 是 → 复用现有文件并更新记录
  ↓ 否
原子保存文件及元数据
  ↓
设置全部显示器壁纸
  ↓
记录成功并执行过期清理
```

### 12.3 手动“立即更新”

- 忽略当天已成功获取的判断；
- 首先请求今日计划来源，必要时按 10.4 规则使用默认来源；
- 不修改自动重试计数和当天自动请求上限；
- 若 Hash 与当前图片相同，不重复保存，提示“壁纸源返回的仍然是当前图片”；
- 若请求失败，保留当前壁纸并显示明确错误。

### 12.4 响应验证

- 仅接受成功 HTTP 响应；
- 支持重定向但须设置有限次数，建议最多 5 次；
- 下载过程中即执行 50 MB 上限保护，不在完整落盘后才判断；
- Content-Type 与实际内容均需校验；
- 图片必须可被 macOS 图像能力成功解码；
- 使用最终解码结果取得宽高和格式；
- 临时文件验证成功后再原子移动至正式缓存位置。

### 12.5 去重

- 当日任务去重键：系统本地自然日 + 任务类型；
- 内容去重依据：完整图片内容 Hash，不依据 URL；
- Hash 已存在时复用原文件，新增或更新当日获取记录；
- 同一图片可关联不同日期或来源请求记录，但只保留一个物理文件。

---

## 13. 失败、重试与默认来源

### 13.1 自动重试

单个计划来源在当天的自动尝试策略：

| 次数 | 失败后的处理 |
|---|---|
| 第 1 次 | 30 分钟后重试 |
| 第 2 次 | 2 小时后重试 |
| 第 3 次 | 当天停止该来源的自动重试，并尝试默认来源一次 |

规则：

- 每天最多对计划来源自动尝试 3 次；
- 用户手动更新不占用自动次数，但必须防止连续点击；
- 应用退出后，重试计划需持久化；下次启动若仍在当天且到期则恢复；
- 跨日后清空前一日未完成重试，开始新一天任务；
- 默认来源失败后不再触发新的默认来源重试链。

### 13.2 连续失败健康状态

- 每个来源记录连续失败天数；
- 某天所有自动及兜底相关尝试失败，才计为该来源当日失败；
- 成功一次后连续失败计数归零；
- 连续 3 天失败时显示警告，但不自动停用或删除来源。

### 13.3 失败保护

任何获取、验证、保存或设置系统壁纸步骤失败时：

- 当前桌面壁纸保持不变；
- 不生成“已成功获取”记录；
- 临时文件应安全清理；
- 错误被记录并可在 UI 查看；
- 应用不得崩溃或进入高频重试。

---

## 14. 壁纸展示模式

### 14.1 每日壁纸模式

- 每个本地自然日成功获取一次今日图片；
- 成功后自动设置到所有显示器；
- 当天不再自动重复请求；
- 手动“立即更新”除外；
- 用户手动“换一张”后，不将今日任务重新标记为未完成。

### 14.2 循环播放模式

- 只使用有效本地壁纸，不请求网络；
- 维持一个低频定时任务；
- 进入睡眠时暂停计时；唤醒后重新计算下次触发，不补播睡眠期间错过的次数；
- 设置变化、Library 删除或清理后重新计算候选列表；
- 候选为空时停止定时器并提示；
- 候选只有当前图片时保持不变，不反复设置系统壁纸。

### 14.3 顺序规则

- 随机：从除当前图片外的候选中等概率选择；
- 按时间顺序：按下载时间从旧到新，时间相同时按 ID 稳定排序；
- 到达末尾后循环回第一张。

### 14.4 手动“换一张”

- 两种模式下均可使用；
- 仅访问本地候选；
- 循环模式沿用当前顺序策略；
- 每日模式默认按时间从新到旧选择下一张历史图片；
- 没有其他候选时提示“没有可切换的其他壁纸”。

---

## 15. 本地缓存与清理

### 15.1 缓存目录

应用仅管理自己的沙盒或 Application Support 数据目录。设置页显示用户友好的逻辑路径，实际路径由 macOS 应用容器决定，不应硬编码为 `~/.zikora/cache`。

推荐逻辑结构：

```text
Wallpapers/
├── 2026-08-18/
│   └── 2026-08-18_sourceId_hash.jpg
└── 2026-08-17/
    └── 2026-08-17_sourceId_hash.png
```

### 15.2 清理时机

- 应用启动完成后；
- 新壁纸下载并设置成功后；
- 用户点击“立即清理”后。

不单独运行高频目录扫描任务。

### 15.3 过期判断

```text
系统当前本地日期 - 壁纸下载日期 > retentionDays
```

“永久”不执行时间过期判断。当前壁纸无条件保护。

### 15.4 一致性规则

- 文件删除成功后再删除对应持久化记录；
- 记录存在但文件丢失时将其排除出候选并在维护任务中清理；
- 文件存在但无记录时不自动删除，避免误删用户数据，记录诊断信息；
- 当前壁纸标记只能有一个；设置新壁纸成功后再切换标记；
- 路径必须位于应用管理目录内，删除前再次校验目标路径。

---

## 16. macOS 系统集成

### 16.1 桌面壁纸

- REQ-SYS-001：将指定的有效本地静态图片设置为 macOS 桌面壁纸。
- REQ-SYS-002：V1.0 对所有已连接显示器设置同一张图片。
- REQ-SYS-003：部分显示器设置失败时记录失败屏幕数量，保留已成功结果并提示用户重试。
- REQ-SYS-004：只有系统设置成功后才更新“当前壁纸”标记。

### 16.2 登录启动

- 默认开启；
- 遵循 macOS 当前版本推荐的登录项机制；
- 后台启动不主动弹窗；
- 用户可在 Settings 中随时关闭。

### 16.3 睡眠与唤醒

- 监听系统睡眠和唤醒事件；
- 唤醒后比较持久化的最后检查日期与当前本地日期；
- 每日模式跨日则执行今日检查，否则不请求网络；
- 循环模式恢复低频任务，不补执行多次切换。

### 16.4 系统日期与时区变化

- 以 macOS 当前本地日历计算“今天”和星期；
- 日期或时区变化后，在下一个系统事件或应用激活时重新检查；
- 同一本地日期已有成功任务时不重复自动下载；
- 系统时间回拨不得触发高频重复任务。

---

## 17. 页面状态与异常文案

| 状态 | 触发条件 | 页面表现 | 可用操作 |
|---|---|---|---|
| Loading | 正在测试、下载、扫描或设置 | 保留现有内容，显示进度；相关按钮禁用 | 允许取消时显示取消 |
| Success | 操作成功 | 更新内容并显示短暂成功提示 | 继续操作 |
| Empty Source | 无任何来源 | “还没有壁纸源” | 添加壁纸源 |
| Empty Library | 无历史壁纸 | “还没有下载过壁纸” | 获取今日壁纸 |
| Offline | 系统无网络 | 保留当前壁纸，显示离线状态 | 网络恢复后重试、手动重试 |
| Source Error | 请求或图片校验失败 | 显示具体原因和最近失败时间 | 重新获取、编辑来源 |
| Disabled | 来源被停用 | 卡片降级显示，计划引用警告 | 重新启用、编辑 |
| Warning | 连续 3 天失败 | 来源卡片显示警告 | 测试、编辑、重试 |
| Missing Plan | 当天未配置且无默认来源 | Dashboard 显示缺少配置 | 前往 Sources 配置 |
| No Candidate | 循环模式无有效图片 | 停止定时任务并提示 | 获取今日壁纸、打开 Library |
| Storage Error | 文件无法保存或删除 | 显示失败数量/原因 | 重试、打开缓存目录 |

错误文案应说明“发生了什么”和“用户可以做什么”，不得仅显示错误代码。

---

## 18. 数据模型

### 18.1 WallpaperSource

| 字段 | 类型/说明 |
|---|---|
| id | 唯一标识 |
| name | 来源名称 |
| url | 远程直链 URL |
| isEnabled | 是否启用 |
| createdAt / updatedAt | 创建/更新时间 |
| lastFetchAt | 最近请求时间，可空 |
| lastFetchStatus | never/loading/success/offline/failed |
| lastErrorCode / lastErrorMessage | 最近错误，可空 |
| consecutiveFailureDays | 连续失败天数 |

### 18.2 Wallpaper

| 字段 | 类型/说明 |
|---|---|
| id | 唯一标识 |
| sourceID | 来源 ID；来源删除后可空 |
| sourceNameSnapshot | 下载时来源名称快照 |
| localPath | 应用管理目录内相对路径 |
| contentHash | 图片内容 Hash，唯一索引 |
| downloadDate | 本地自然日 |
| createdAt | 精确创建时间 |
| width / height | 像素尺寸 |
| fileSize | 字节数 |
| format | jpg/png/heic/webp 等 |
| isCurrent | 是否为当前壁纸，最多一条为真 |

### 18.3 WeeklySchedule

```text
mondaySourceID
tuesdaySourceID
wednesdaySourceID
thursdaySourceID
fridaySourceID
saturdaySourceID
sundaySourceID
defaultSourceID
```

所有来源引用允许为空。读取到不存在的 ID 时按“未设置”处理。

### 18.4 UserSettings

```text
launchAtLogin: Bool = true
wallpaperMode: daily | slideshow = daily
retentionDays: 7 | 14 | 30 | 60 | 90 | forever = 30
slideshowOrder: random | chronological = random
slideshowInterval: 5m | 15m | 30m | 1h | 3h | 6h | 12h | 1d = 30m
onboardingCompleted: Bool
lastSelectedNavigation: dashboard | sources | library | settings
```

### 18.5 DailyFetchRecord

为保证幂等与重试恢复，补充每日任务记录：

```text
id
localDate
plannedSourceID
actualSourceID
status: pending | retryScheduled | success | failed
automaticAttemptCount
usedDefaultSource
wallpaperID
lastAttemptAt
nextRetryAt
lastErrorCode
```

---

## 19. 非功能需求

### 19.1 性能

- NFR-PERF-001：空闲 CPU 接近 0%，不得存在持续忙轮询。
- NFR-PERF-002：空闲内存目标低于 80 MB，理想范围 30～60 MB。
- NFR-PERF-003：Library 使用缩略图，不在网格中同时解码全部原图。
- NFR-PERF-004：图片 Hash、解码、磁盘扫描不得阻塞主线程。
- NFR-PERF-005：大量历史记录时采用懒加载，至少应支持 1,000 张记录正常浏览。

### 19.2 网络

- NFR-NET-001：循环播放不产生任何壁纸网络请求。
- NFR-NET-002：每日模式遵循当日去重和最多 3 次自动尝试规则。
- NFR-NET-003：请求设置合理超时、重定向上限和 50 MB 大小上限。
- NFR-NET-004：网络恢复事件应合并处理，避免并发启动重复任务。

### 19.3 稳定性

- NFR-STAB-001：应用重启后配置、当前壁纸、每日任务和待重试状态可恢复。
- NFR-STAB-002：网络失败、图片损坏、磁盘不足、权限失败不导致应用崩溃。
- NFR-STAB-003：下载使用临时文件和原子写入，避免产生半文件。
- NFR-STAB-004：同一时间最多运行一个全局获取任务。

### 19.4 安全与隐私

- NFR-SEC-001：不要求账号，不上传来源配置、本地图片或使用行为。
- NFR-SEC-002：只管理应用自己的数据目录，不删除目录外文件。
- NFR-SEC-003：网络响应必须经过大小、类型和解码校验。
- NFR-SEC-004：日志不得记录图片二进制数据；URL 如含查询参数，错误展示和日志应避免暴露敏感参数。
- NFR-SEC-005：隐私说明中列明应用会访问用户配置的远程地址。

### 19.5 可访问性与易用性

- NFR-A11Y-001：所有图标按钮提供可读标签或 Tooltip。
- NFR-A11Y-002：状态不能只靠颜色表达，需同时使用文字或图标。
- NFR-A11Y-003：支持键盘焦点、Tab 导航和 Enter/Space 激活常用控件。
- NFR-A11Y-004：文本与背景满足 macOS 常规可读性要求，图片浮层需保证文字对比度。
- NFR-A11Y-005：界面跟随系统减少动态效果设置，非必要悬浮缩放可关闭。

---

## 20. UI 视觉与适配要求

原型采用 Vivid Lumina 视觉语言，V1.0 实现应保留以下特征：

- 浅色背景、半透明面板、适度模糊与柔和层级；
- Electric Blue / `#007AFF` 作为主交互色；
- 圆角卡片、胶囊按钮及清晰的选中态；
- Dashboard 图片优先，避免堆叠无意义数据卡；
- 使用 macOS 原生交互习惯，避免网页式过度 hover 动效；
- 主窗口优先适配桌面尺寸，窄窗口时内容可重排，但不得丢失操作；
- 周计划、设置区和 Library 在内容超出时可滚动；
- 所有原型英文文案在中文版本中按本文档统一，不保留中英混杂。

原型中的玻璃拟态、阴影和动画属于视觉规范，不应以牺牲空闲性能、可读性或系统“减少透明度/动态效果”偏好为代价。

---

## 21. 埋点、日志与诊断

V1.0 不上传分析数据。仅在本地记录必要诊断信息：

- 应用启动/退出；
- 自动任务触发原因；
- 来源 ID、请求开始/结束、结果和耗时；
- 图片验证失败类型；
- Hash 去重结果；
- 系统壁纸设置结果；
- 缓存清理数量和失败数量；
- 重试计划创建、恢复和取消。

日志应有容量或时间轮转策略，不得无限增长。用户应可在“关于”或更多菜单中打开诊断日志位置（P1）。

---

## 22. 验收标准

### 22.1 首次引导

- [ ] 无配置首次启动时进入引导，而非空白 Dashboard。
- [ ] 未测试成功的 URL 不能完成引导。
- [ ] 完成引导后首个来源被设置为七天计划及默认来源。
- [ ] 默认设置为登录启动开启、每日模式、保留 30 天。

### 22.2 壁纸源

- [ ] 可添加、编辑、删除、启用和停用远程直链来源。
- [ ] URL 修改后必须重新测试才能保存。
- [ ] 测试能识别超时、HTTP 失败、非图片、无法解码和大于 50 MB。
- [ ] 测试成功显示预览及图片信息。
- [ ] 删除来源不删除历史壁纸，并清理计划和默认来源的悬空引用。

### 22.3 每周计划与兜底

- [ ] 周一至周日均可独立选择已启用来源或未设置。
- [ ] 今日来源未设置、被删除或停用时，可使用默认来源。
- [ ] 计划来源三次自动失败后最多尝试默认来源一次。
- [ ] 默认来源与计划来源相同时不重复请求。

### 22.4 自动获取

- [ ] 应用启动、登录启动、跨日唤醒可触发今日检查。
- [ ] 当天成功后不会自动重复下载。
- [ ] 手动“立即更新”可绕过当日去重。
- [ ] 相同内容通过 Hash 复用文件。
- [ ] 下载、验证或保存失败时当前桌面壁纸不变且应用不崩溃。
- [ ] 自动失败按 30 分钟、2 小时、当天停止的策略执行。

### 22.5 展示模式

- [ ] 每日模式成功获取后设置全部显示器为同一图片。
- [ ] 循环模式支持随机、按时间顺序及全部规定间隔。
- [ ] 循环模式不产生网络请求。
- [ ] 候选数大于 1 时不会连续显示同一张。
- [ ] 睡眠唤醒后不补播多次切换。
- [ ] 主窗口与 Menu Bar 均可“换一张”。

### 22.6 Library

- [ ] 网格展示缩略图、日期、来源和当前壁纸标识。
- [ ] 默认按下载时间倒序排列。
- [ ] 点击图片进入详情而非直接换壁纸。
- [ ] 可将历史图片设为当前壁纸。
- [ ] 当前壁纸不可删除。
- [ ] 删除其他壁纸后文件和记录一致更新。

### 22.7 缓存

- [ ] 可选择 7/14/30/60/90 天或永久。
- [ ] 应用启动、下载成功、手动操作可触发清理。
- [ ] 当前壁纸即使过期也不会被删除。
- [ ] 设置页能显示缓存占用并在 Finder 打开缓存目录。
- [ ] “立即清理”先展示影响并要求确认。

### 22.8 系统与后台

- [ ] 登录启动默认开启且不自动弹出主窗口。
- [ ] 关闭主窗口后 Menu Bar 与后台任务继续运行。
- [ ] 只有“退出”真正终止应用。
- [ ] 睡眠、唤醒、网络变化和日期变化后逻辑正常。
- [ ] 空闲 CPU 接近 0%，空闲内存低于 80 MB。

### 22.9 UI 状态

- [ ] 四个主页面与导航状态和原型一致。
- [ ] 关键流程均有 Loading、Success、Empty、Offline、Error、Disabled、Warning、Missing Configuration 状态。
- [ ] 无响应的装饰按钮为 0；所有图标按钮均有标签或 Tooltip。
- [ ] 状态不只依赖颜色传达。

---

## 23. 需求追踪矩阵

| 需求域 | PRD 章节 | 原型依据 | 本文档章节 |
|---|---|---|---|
| Dashboard | 15、16.1 | `dashboard_vibe_update` | 10.1 |
| Sources | 5、16.2、16.3 | `sources_vibe_update` | 10.2 |
| Weekly Plan | 6、16.4 | `sources_vibe_update` | 10.3 |
| Default Source | 6.4 | `sources_vibe_update` | 10.4 |
| Library / Detail | 16.5、16.6 | `library_vibe_update` | 10.5、10.6 |
| Settings | 9～12、13.3、16.7 | `settings_vibe_update` | 10.7 |
| Menu Bar | 14 | 暂无原型 | 11 |
| 自动获取与重试 | 7、18、19、24 | 页面状态补充 | 12、13 |
| 缓存与清理 | 8、9 | Settings 原型 | 15 |
| 系统集成 | 13、20 | Settings 原型 | 16 |
| 非功能需求 | 21、22、25、27 | Vivid Lumina 设计规范 | 19、20 |

---

## 24. 发布前待补设计资产

以下内容在 PRD 中已要求，但当前 `prototype/` 未提供完整原型，进入开发前应补齐设计或确认采用 macOS 原生样式：

1. 首次引导三步；
2. 添加/编辑壁纸源 Sheet 及测试成功/失败状态；
3. 壁纸详情；
4. Menu Bar 菜单；
5. 删除来源、删除壁纸、立即清理确认框；
6. 无来源、无历史、离线、连续失败、缺少计划等状态；
7. Sources 页面周六、周日完整状态；
8. Settings 的 About 区域；
9. 主窗口在较窄尺寸下的布局；
10. 系统“减少透明度”和“减少动态效果”下的降级样式。

这些缺失不改变本文档定义的业务范围，设计与开发应以本文档中的功能、状态和验收条件为准。
